# DEPRECATED — this drop-in is no longer active.
#
# XVF3510 initialization is now handled by mark2-audio-init.service
# which runs mark2-xvf-post-wp.sh at boot.
#
# The old approach (ExecStartPost re-flash on every WirePlumber start)
# was replaced because the real fix is setting MCLK=12.288MHz BEFORE
# flash, not re-flashing after WirePlumber opens the device.
#
# See docs/XVF3510_HARDWARE.md for full explanation.
