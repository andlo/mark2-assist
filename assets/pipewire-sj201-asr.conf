# INTENTIONALLY EMPTY — not used
# We follow the OVOS installer approach: no custom PipeWire virtual source.
# WirePlumber's pro-audio profile (90-sj201-profile.conf) exposes
# alsa_input.platform-soc_sound.pro-input-1 ("Built-in Audio Pro 1")
# which LVA uses directly. No separate virtual node needed.
